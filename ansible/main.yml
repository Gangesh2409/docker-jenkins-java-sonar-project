---
- hosts: localhost
  connection: 107.23.14.177
  remote_user: ubuntu
  tasks:
    - name: Create a java container
      community.docker.docker_container:
        name: java
        image: saidamo/java:apps
        state: started
        restart: yes
        ports:
          - "80:8080"
